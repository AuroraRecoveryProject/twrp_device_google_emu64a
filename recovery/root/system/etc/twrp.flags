# TWRP partition flags — QEMU virtio-blk, no dynamic partitions
#
# <mnt_point>  <fstype>  <block_device>                    <flags>
/data          ext4      /dev/block/vdc                    flags=backup=1
/sdcard        auto      /devices/*/block/vdf              flags=display="SD Card";storage;wipe=false
